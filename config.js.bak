/**
 * Config untuk bot-stok.
 * Semua nilai hardcoded dipusatkan di sini.
 * Bisa di-import dari semua file JS dengan require('./config.js')
 */

const path = require('path');

const config = {
    // === API KLIK INDOMARET ===
    AP_MC_BASE: "https://ap-mc.klikindomaret.com",
    SEARCH_PATH: "/assets-klikidmcore/api/get/catalog-xpress/api/webapp/search/result",

    // === DEFAULT STORE ===
    DEFAULT_STORE_CODE: "TX0B",
    STORE_COORDS: {
        TX0B: { latitude: "-6.1134", longitude: "106.8828", mode: "PICKUP", districtId: "317305100" },
        TJKT: { latitude: "-6.1763897", longitude: "106.82667", mode: "DELIVERY", districtId: "141100100" },
        "2449": { latitude: "-6.1134", longitude: "106.8828", mode: "PICKUP", districtId: "317305100" },
    },

    // === CDN & DOMAIN ===
    CDN_PRODUCT_URL: "https://cdn-klik.klikindomaret.com/klik-catalog/product",
    BARCODE_API_URL: "https://barcode.klikidm.my.id/api",
    WEBSITE_URL: "https://klikidm.my.id",
    API_REFERER: "https://www.klikindomaret.com/",
    API_ORIGIN: "https://www.klikindomaret.com",

    // === SUPABASE (fallback gambar) ===
    SUPABASE_URL: "https://rurnqvmyboriislxfmwr.supabase.co",
    SUPABASE_KEY: "", // ISI DENGAN KEY ASLI

    // === BARCODE DATABASE ===
    BARCODE_DB_PATH: "/sdcard/Download/barcodesheet.json",

    // === GITHUB ===
    GITHUB_TOKEN: "", // ISI DENGAN TOKEN ASLI
    GITHUB_TOKEN_UPGH: "", // ISI DENGAN TOKEN GITHUB PAT ASLI (untuk tools-upgh.js)
    GITHUB_REPO_BARCODES: { owner: "Marsudi505", repo: "Barcodes", path: "barcodesheet.json" },
    GITHUB_REPO_TOKO: { owner: "Marsudi505", repo: "toko-idm", path: "toko.json" },

    // === FILE PATHS ===
    BARCODE_DB: "/sdcard/Download/barcodesheet.json",
    FILE_ALLOWED_USERS: "./allowed_users.json",
    FILE_USER_MODE: "./usermode.json",
    SESSION_AUTH_DIR: "./session_auth",
    PLU_HELPER_PATH: path.join(process.cwd(), "plu_helper.py"),

    // === IMAGE / CANVAS ===
    CANVAS_WIDTH: 1000,
    CANVAS_HEIGHT: 1448,
    PRODUCT_MAX_W: 800,
    PRODUCT_MAX_H: 1100,
    BARCODE_MAX_W: 800,
    BARCODE_MAX_H: 150,

    // === TIMEOUTS (ms) ===
    TIMEOUT_API: 20000,
    TIMEOUT_IMAGE: 10000,
    TIMEOUT_COMBINE: 30000,

    // === OWNER ===
    OWNER_PHONE: "085790374090",
    OWNER_NUMBER: "6285790374090",
    OWNER_BC_A: "7901479538", // BCA QRIS

    // === MENU / QRIS IMAGES ===
    MENU_IMAGE_URL: "https://files.catbox.moe/gdg5yl.jpg",
    QRIS_IMAGE_URL: "https://files.catbox.moe/gdg5yl.jpg",

    // === BOT META ===
    BOT_NAME: "INDICA PROJECT",
    BOT_VERSION: "V20.9",
    BOT_SIGNATURE: "© INDICA PROJECT est. 2026",
};

module.exports = config;